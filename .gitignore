node_modules/
dist/
.env
.env.*
!.env.example

# Base locale PGlite : ne jamais versionner de donnees clients.
# Le motif couvre aussi les copies mises de cote (.pgdata.casse, .pgdata-test).
backend/.pgdata*/

# Ancienne base lowdb, remplacee par Postgres
backend/data/

# Photos locales : en production elles vivent chez Cloudinary
uploads/

.DS_Store
Thumbs.db
*.log

# Fichiers sources bruts (logos, photos avant traitement).
# L app utilise les versions optimisees de frontend/public/.
image/
